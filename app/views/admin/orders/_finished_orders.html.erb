<%= link_to "Pasūtījumi", admin_orders_path %>

<table style="width:100%">
  <tr>
    <td>Id</td>
    <td>Pasūtīts</td>
    <td>Pasūtījums pabeigts</td>
    <td>Produkti</td>    
    <td>Summa</td>
  </tr>
  <%- @orders.each do |order| %>
    <tr>
      <td><%= order.id %></td>
      <td><%= order.created_at.strftime("%d/%m/%Y - %I:%M") %></td>
      <td><%= order.updated_at.strftime("%d/%m/%Y - %I:%M") %></td>
      <td> 
        <%- order.products.each do |product| %>
          <%= product.name %>
          <br/>
        <% end %>
      </td>
      <td><%= price_in_euro(order.total_price) %></td>  
      </td>
    </tr>
  <% end %>
</table>